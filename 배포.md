# 깃허브

## 배포 git 순서
```js
     git init
     git config user.name 'moonjongjs'
     git config user.email 'moonseonjong@naver.com'
     git add .
     git commit -m '웨딩 웹페지이 배포'
     git remote add origin https://github.com/moonjongjs/wedding_.git
     git push origin master
```